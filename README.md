# EmpireOne Health Next.js Project


## Run Locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

The original `.html` files remain in the project root and are rendered by the Next catch-all route. Assets are served from `public/assets`.
